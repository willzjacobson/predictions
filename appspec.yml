version: 0.0
os: linux 

files:
  - source: /
    destination: /var/analytics/an_predictions

hooks:
  BeforeInstall:
    - location: /bash_scripts/before_install.sh
  AfterInstall:
    - location: /bash_scripts/after_install.sh